enum Filter {
  HIDE_NOT_SUPPORTED_IN_US = "isSupportedInUS",
  HIDE_NOT_SUPPORTED_TEST_MODE = "supportsTestMode",
}

enum Sort {
  CODE = "code",
  NAME = "name",
  RANDOM = "random",
}

export { Filter, Sort };
